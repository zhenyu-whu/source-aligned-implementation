## Why

<!-- 说明此 production change 的动机：它解决或解锁了哪个产品、架构、production-readiness 或 verification 缺口？ -->

## What Changes

<!-- 描述此 change 修改的 source-backed 产品行为、生产代码、data/API contracts、routes、objects、jobs、events、auth/security rules、storage、billing/entitlement rules、observability、deployment 与 verification surfaces。引用 Source Truth IDs，例如 ST-001。 -->

## Capabilities

### New Capabilities
<!-- 描述新增 capabilities。将 <name> 替换为 kebab-case identifier；每个 capability 会创建 specs/<name>/spec.md。 -->
- `<name>`: <概述该 capability 覆盖的范围>

### Modified Capabilities
<!-- 描述 REQUIREMENTS 发生变化的 existing capabilities。使用 openspec/specs/ 中已有 spec 名称；没有 requirement 变化时留空。 -->
- `<existing-name>`: <说明变化的 requirement>

## Production Source Coverage

<!-- 列出此 change 必须保持一致的 exact source files、sections、Source Pointers 与 exact Source Truth IDs。内容应来自 consumed ST items 的 Source Pointers / Read Notes，包括 PRD、architecture、prototype docs、API/data/security/ops/deployment references 与 verification sources。不要使用 ST-001-ST-010 这类 Source Truth ID ranges，也不要新增宽泛的整篇文档读取义务。 -->

## Non-Goals

<!-- 明确排除 source-backed later-change boundaries、global forbidden drift 与 out-of-scope work；适用时引用 Source Truth IDs。 -->

## Impact

<!-- 说明受影响的 apps、packages、database migrations、APIs、workers、queues、SSE/outbox events、frontend routes、components、assets、auth/security/privacy boundaries、observability、deployment、verification scripts 与 dependencies。 -->

## Rollout / Readiness

<!-- 说明 migration ordering、environment/config needs、backfill、compatibility、staging/production smoke、operational runbook、monitoring/audit readiness；不适用时写“无”。 -->
