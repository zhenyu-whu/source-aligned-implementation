## Why

<!-- 说明此 prototype/UI change 的动机：它解决或解锁了哪个 prototype dependency、scene gap 或 verification gap？ -->

## What Changes

<!-- 描述此 change 修改的 source-backed behavior、scenes、routes、objects、fixtures、assets、data/API contracts 与 verification surfaces。引用 Source Truth IDs，例如 ST-001。 -->

## Capabilities

### New Capabilities
<!-- 描述新增 capabilities。将 <name> 替换为 kebab-case identifier；每个 capability 会创建 specs/<name>/spec.md。 -->
- `<name>`: <概述该 capability 覆盖的范围>

### Modified Capabilities
<!-- 描述 REQUIREMENTS 发生变化的 existing capabilities。使用 openspec/specs/ 中已有 spec 名称；没有 requirement 变化时留空。 -->
- `<existing-name>`: <说明变化的 requirement>

## Prototype Source Coverage

<!-- 列出此 change 必须保持一致的 exact source files、sections 与 exact Source Truth IDs。包括 plan row、PRD/architecture/prototype docs、page/object specs 与 system readiness docs。不要使用 ST-001-ST-010 这类 Source Truth ID ranges。 -->

## Non-Goals

<!-- 明确排除 source-backed later-change boundaries、global forbidden drift 与 out-of-scope work；适用时引用 Source Truth IDs。 -->

## Impact

<!-- 说明受影响的 prototype code areas、deterministic fixtures、assets、interactions、verification scripts 与 dependencies。 -->
